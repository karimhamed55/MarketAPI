# Market API

A RESTful Web API built with **.NET 8** and **Entity Framework Core** to manage market products. This project demonstrates full CRUD operations (Create, Read, Update, Delete) using a SQL Server database.

## 🚀 Tech Stack
* **Framework:** .NET 8 Web API
* **Language:** C#
* **Database:** SQL Server
* **ORM:** Entity Framework Core
* **Documentation:** Swagger / OpenAPI

## 📂 Project Structure
* `Controllers/`: Contains the API endpoints (`MarketController`).
* `Data/`: Database context and configuration.
* `Models/`: Entity definitions (`Product.cs`).
* `postman/`: Contains the exported Postman collection for testing.
